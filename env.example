# Client app environment (no secrets here)
# URL of the proxy server you will run/deploy
# Para Expo Web em produção (Vercel), use EXPO_PUBLIC_API_BASE_URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:8787
# Fallback para compatibilidade
API_BASE_URL=http://localhost:8787

# Optional client hint; server uses BRAPI_API_TOKEN in its own env
BRAPI_API_TOKEN=

